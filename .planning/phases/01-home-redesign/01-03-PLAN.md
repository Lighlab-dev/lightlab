---
phase: 01-home-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/pages/Home.tsx
  - src/i18n.tsx
autonomous: false
requirements:
  - WHY-LIGHTLAB
  - FINAL-CTA-DUAL-BUTTONS
  - SECTION-ORDER-9

must_haves:
  truths:
    - "Why LIGHTLAB section exists between Testimonials and Final CTA"
    - "Why LIGHTLAB shows 5 differentiation points: engineering-first, performance-focused, long-term partnership, data-driven, built for scale"
    - "Final CTA section has two buttons: primary 'Start a Project' (mailto) and secondary 'Book a Diagnostic' (/contact)"
    - "The 9-section page order matches the spec: Hero → Trust Signals → Services → How We Work → Featured Projects → Performance Metrics → Testimonials → Why LIGHTLAB → Final CTA"
    - "All new copy is sourced from i18n, not hardcoded"
    - "Page compiles and loads without TypeScript or runtime errors"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "WhyLightlab section; updated CTA section with two buttons; correct 9-section order"
      contains: "WHY LIGHTLAB"
    - path: "src/i18n.tsx"
      provides: "whyLightlab translations for en/fr/ar; ctaButton2 key for en/fr/ar; heroCtaSecondary key for en/fr/ar"
      contains: "whyLightlab"
  key_links:
    - from: "src/pages/Home.tsx WhyLightlab section"
      to: "src/i18n.tsx copy.home.whyLightlab"
      via: "useLanguage() hook"
      pattern: "copy\\.home\\.whyLightlab"
    - from: "Final CTA primary button"
      to: "src/i18n.tsx copy.home.ctaButtonStart"
      via: "useLanguage() hook"
      pattern: "copy\\.home\\.ctaButtonStart"
    - from: "Final CTA secondary button"
      to: "/contact route"
      via: "MagneticButton href"
      pattern: "href=\"/contact\""
---

<objective>
Add the "Why LIGHTLAB" differentiation section (section 8 of 9) and upgrade the Final CTA to include both primary and secondary action buttons. This completes all 9 sections per the spec.

Purpose: "Why LIGHTLAB" is the closing argument before the CTA — it articulates LIGHTLAB's five differentiators (engineering-first, performance-focused, etc.) to convert visitors who have read the full page. The CTA needs two options to reduce friction: a direct start ("Start a Project") and a softer entry ("Book a Diagnostic").
Output: A new WhyLightlab inline component, updated Final CTA with two MagneticButtons, and all i18n keys for EN/FR/AR.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-home-redesign/01-CONTEXT.md
@src/pages/Home.tsx
@src/components/PremiumUI.tsx
@src/i18n.tsx
@.planning/phases/01-home-redesign/01-home-redesign-01-SUMMARY.md
@.planning/phases/01-home-redesign/01-home-redesign-02-SUMMARY.md

<interfaces>
<!-- After plans 01 and 02, i18n.tsx already has: -->
```typescript
copy.home.ctaButtonStart   // 'Start a Project' — use for the Final CTA primary button
copy.home.ctaButton        // 'Book a Diagnostic' — use for the Final CTA secondary button
copy.home.trustSignals     // added in plan 01
copy.home.howWeWork        // added in plan 02
copy.home.serviceTracks    // now 4 entries
```

<!-- After plans 01 and 02, the section order in Home.tsx JSX is: -->
<!--   Hero → TrustSignals → Services → HowWeWork → Projects → Stats → Testimonials → CTA -->
<!-- (METHOD was removed in plan 02) -->
<!-- Target after this plan: -->
<!--   Hero → TrustSignals → Services → HowWeWork → Projects → Stats → Testimonials → WhyLightlab → CTA -->

<!-- New i18n shapes to add in this plan -->
```typescript
copy.home.whyLightlab: {
  label: string
  headline: string
  pillars: Array<{ title: string; copy: string }>   // 5 items
}
copy.home.ctaButton2: string   // secondary CTA label in Final CTA — 'Book a Diagnostic'
copy.home.heroCtaSecondary: string  // Hero secondary CTA — 'View Our Work'
```

<!-- WhyLightlab component must be defined ABOVE the Home function — no closure. -->
interface WhyLightlabProps {
  isDark: boolean
  isArabic: boolean
  P: typeof LT
  dir: { text: string; flex: string }
  copy: ReturnType<typeof useLanguage>['copy']
}

<!-- MagneticButton already supports href + variant props -->
```typescript
<MagneticButton href="/contact" variant="outline" isDark>
  Book a Diagnostic
</MagneticButton>
```

<!-- Final CTA primary button: -->
<!-- Currently: {copy.home.ctaButton} which reads 'Book a Diagnostic' -->
<!-- After plan 01: the Hero primary was changed to ctaButtonStart. -->
<!-- In this plan, also update the Final CTA primary to ctaButtonStart ('Start a Project'). -->
<!-- The secondary button uses ctaButton2 ('Book a Diagnostic'). -->
<!-- ctaButton (old key) is NOT used in Home.tsx after this plan — but keep it in i18n. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add whyLightlab, ctaButton2, and heroCtaSecondary i18n keys</name>
  <files>src/i18n.tsx</files>
  <action>
In src/i18n.tsx, add to the `home` namespace in ALL THREE locales. Place after `howWeWork` (added in plan 02). Add only — do not rename or remove existing keys.

**A — Add `heroCtaSecondary`** (Hero secondary CTA, "View Our Work"):

EN: `heroCtaSecondary: 'View Our Work',`
FR: `heroCtaSecondary: 'Voir nos réalisations',`
AR: `heroCtaSecondary: 'عرض أعمالنا',`

**B — Add `ctaButton2`** (Final CTA secondary button):

EN: `ctaButton2: 'Book a Diagnostic',`
FR: `ctaButton2: 'Réserver un Diagnostic',`
AR: `ctaButton2: 'احجز تشخيصاً',`

**C — Add `whyLightlab`**:

EN:
```
whyLightlab: {
  label: 'Why LIGHTLAB',
  headline: 'Built different. By design.',
  pillars: [
    {
      title: 'Engineering-First',
      copy: 'Every engagement starts with architecture. We build systems that scale, not prototypes that break.',
    },
    {
      title: 'Performance-Focused',
      copy: 'We measure success in revenue impact, not deliverables. Every sprint ships against a KPI.',
    },
    {
      title: 'Long-Term Partnership',
      copy: 'We operate as an embedded product team — aligned to your roadmap, not billable hours.',
    },
    {
      title: 'Data-Driven',
      copy: 'Attribution, analytics, and feedback loops are built into every system from day one.',
    },
    {
      title: 'Built for Scale',
      copy: 'Infrastructure decisions are made for 10x growth. We never build what you will need to rebuild.',
    },
  ],
},
```

FR:
```
whyLightlab: {
  label: 'Pourquoi LIGHTLAB',
  headline: 'Construit différemment. Par conception.',
  pillars: [
    { title: 'Engineering en premier', copy: 'Chaque engagement commence par l\'architecture. Nous construisons des systèmes qui évoluent, pas des prototypes qui cassent.' },
    { title: 'Axé sur la performance', copy: 'Nous mesurons le succès par l\'impact sur les revenus, pas par les livrables. Chaque sprint livre contre un KPI.' },
    { title: 'Partenariat à long terme', copy: 'Nous opérons comme une équipe produit intégrée — alignée sur votre roadmap, pas sur les heures facturables.' },
    { title: 'Basé sur les données', copy: 'L\'attribution, l\'analytique et les boucles de feedback sont intégrés dans chaque système dès le premier jour.' },
    { title: 'Construit pour l\'échelle', copy: 'Les décisions d\'infrastructure sont prises pour une croissance 10x. Nous ne construisons jamais ce que vous devrez reconstruire.' },
  ],
},
```

AR:
```
whyLightlab: {
  label: 'لماذا LIGHTLAB',
  headline: 'مبني بشكل مختلف. بالتصميم.',
  pillars: [
    { title: 'الهندسة أولاً', copy: 'كل مشاركة تبدأ بالهندسة المعمارية. نبني أنظمة قابلة للتوسع، وليس نماذج أولية تنكسر.' },
    { title: 'التركيز على الأداء', copy: 'نقيس النجاح بتأثير الإيرادات، وليس بالمخرجات. كل سبرينت يُسلَّم مقابل مؤشر أداء رئيسي.' },
    { title: 'شراكة طويلة الأمد', copy: 'نعمل كفريق منتج مدمج — متوافق مع خارطة طريقك، وليس الساعات القابلة للفوترة.' },
    { title: 'مدفوع بالبيانات', copy: 'الإسناد والتحليلات وحلقات التغذية الراجعة مدمجة في كل نظام من اليوم الأول.' },
    { title: 'مبني للتوسع', copy: 'قرارات البنية التحتية تُتخذ لنمو 10x. لا نبني أبداً ما ستحتاج إلى إعادة بنائه.' },
  ],
},
```
  </action>
  <verify>
    <automated>cd /c/Users/PC/Documents/Github/lightlab && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. `copy.home.whyLightlab.pillars` has 5 items, `copy.home.ctaButton2` exists, and `copy.home.heroCtaSecondary` exists in all three locales.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build WhyLightlab section, update Final CTA buttons, fix Hero secondary CTA</name>
  <files>src/pages/Home.tsx</files>
  <action>
Make three targeted changes to Home.tsx:

**Change A — Fix Hero secondary CTA label:**
The Hero secondary `MagneticButton` (variant="outline", `href="/services"`) currently renders `{copy.nav.services}`. Change it to render `{copy.home.heroCtaSecondary}` (which now equals "View Our Work" in EN). The href can stay as `/services` or be changed to `/projects` — keep `/services` since it is the existing value.

**Change B — Insert WhyLightlab section:**
Create a new inline memo component `WhyLightlab` (above the Home function, below the HowWeWork component). It receives all data as props — defined outside Home, it cannot use closure over Home's local variables.

Props interface:
```typescript
interface WhyLightlabProps {
  isDark: boolean
  isArabic: boolean
  P: typeof LT
  dir: { text: string; flex: string }
  copy: ReturnType<typeof useLanguage>['copy']
}
```

Insert `<WhyLightlab isDark={isDark} isArabic={isArabic} P={P} dir={dir} copy={copy} />` AFTER the closing `</LazySection>` of the Testimonials section and BEFORE the Final CTA `<LazySection id="contact">`.

WhyLightlab layout:
- Outer: `<LazySection id="why-lightlab">`
- Inner: `border-t ${P.border}` > `max-w-7xl mx-auto px-6 md:px-12 lg:px-20 py-20 md:py-28`
- Header: same pattern as other sections (eyebrow + SplitText headline)
  - `copy.home.whyLightlab.label` as eyebrow
  - `copy.home.whyLightlab.headline` as SplitText (type="words")
- Pillars grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mt-12`
- Map over `copy.home.whyLightlab.pillars` (5 items — grid auto-wraps as 3+2):
  ```tsx
  <div className={`flex flex-col gap-4 p-8 rounded-2xl border
    ${isDark
      ? `${P.cardBg} ${P.border} hover:bg-white/[0.06]`
      : `${P.cardBg} ${P.border} hover:bg-[#f5f2ea]`}
    transition-colors duration-300 group`}>
    {/* Index dot */}
    <span className={`w-1.5 h-1.5 rounded-full ${isDark ? 'bg-white/20' : 'bg-[#d9d5ca]'}`} />
    {/* Title */}
    <h4 className={`text-lg md:text-xl font-display font-light leading-tight
      ${isDark ? 'text-white/85' : 'text-[#18160f]'}`}>
      {pillar.title}
    </h4>
    {/* Copy */}
    <p className={`text-sm font-light leading-[1.72]
      ${isDark ? 'text-white/40' : 'text-[#56514a]'}`}>
      {pillar.copy}
    </p>
  </div>
  ```

**Change C — Update Final CTA buttons:**
Find the Final CTA section (currently has a single `MagneticButton` with `href="mailto:hello@lightlab.dev"`).

Step 1 — Update the existing primary button's label:
Change `{copy.home.ctaButton}` to `{copy.home.ctaButtonStart}`. This changes it from "Book a Diagnostic" to "Start a Project". Keep the href (`mailto:hello@lightlab.dev`), variant (`primary`), and the arrow SVG unchanged.

Step 2 — Add a secondary button immediately after the primary button:
```tsx
<MagneticButton
  href="/contact"
  variant="outline"
  isDark
  className={`inline-flex items-center gap-3 px-8 py-4 rounded-full
    text-[11px] font-bold tracking-[0.24em] uppercase
    border-white/15 text-white/60 hover:text-white
    ${isArabic ? 'flex-row-reverse' : ''}`}
>
  {copy.home.ctaButton2}
</MagneticButton>
```

Step 3 — Wrap both buttons in a flex container:
```tsx
<div className={`flex flex-wrap gap-4 items-center ${isArabic ? 'flex-row-reverse' : ''}`}>
  {/* primary button */}
  {/* secondary button */}
</div>
```

IMPORTANT: `copy.home.ctaButtonStart` resolves to "Start a Project" (added in plan 01). `copy.home.ctaButton2` resolves to "Book a Diagnostic" (added in Task 1 of this plan). These are two separate, unambiguous i18n keys. Do NOT use `copy.nav.cta` or any fallback — use only `ctaButtonStart` for primary and `ctaButton2` for secondary.

DO NOT change Hero (beyond the secondary CTA fix in Change A), Trust Signals, Services, How We Work, Featured Projects, Stats, or Testimonials sections.
  </action>
  <verify>
    <automated>cd /c/Users/PC/Documents/Github/lightlab && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes. In the browser at localhost:5173:
- "Why LIGHTLAB" section with 5 differentiation pillar cards appears after Testimonials and before Final CTA
- Final CTA section shows two buttons side by side: primary dark button "Start a Project" and secondary outline button "Book a Diagnostic"
- Hero secondary CTA reads "View Our Work"
- All 9 sections present and in correct order: Hero, Trust Signals, Services, How We Work, Featured Projects, Performance Metrics, Testimonials, Why LIGHTLAB, Final CTA
- Language switching works for new sections in all 3 locales
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of the complete 9-section home page</name>
  <files>src/pages/Home.tsx</files>
  <action>Human visual verification — no code changes. Run npm run dev and inspect the complete page.</action>
  <verify><automated>cd /c/Users/PC/Documents/Github/lightlab &amp;&amp; npm run build 2&gt;&amp;1 | tail -5</automated></verify>
  <done>User confirms premium quality bar is met across all 9 sections, both themes, and all 3 languages.</done>
  <what-built>All 9 home page sections are now built. The complete conversion-optimized page includes: Hero (primary CTA "Start a Project", secondary "View Our Work"), Trust Signals, Services (4 pillars), How We Work (4 steps), Featured Projects (with result metrics), Performance Metrics, Testimonials, Why LIGHTLAB (5 pillars), and Final CTA (primary "Start a Project" + secondary "Book a Diagnostic").</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:5173
2. Scroll the full page top to bottom — verify all 9 sections render (no METHOD section)
3. Verify Hero primary CTA reads "Start a Project" and secondary reads "View Our Work"
4. Switch to dark mode (theme toggle in nav) — verify all sections look correct
5. Switch language to FR — verify Trust Signals, How We Work, and Why LIGHTLAB show French copy
6. Switch language to AR — verify RTL layout applies correctly across all sections
7. Hover over project cards — verify the result stat fades in (e.g. "+52% Leads")
8. Scroll to Final CTA — verify two buttons ("Start a Project" and "Book a Diagnostic") appear side by side
9. Check mobile viewport (375px width) — verify sections stack correctly, no overflow
10. Run `npm run build` — confirm zero errors
  </how-to-verify>
  <resume-signal>Type "approved" if the page meets the premium quality bar (Linear / Stripe / Vercel level). Describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
npm run build passes with zero errors. All 9 sections present in correct order. Hero CTAs correct. Final CTA has two buttons. Verified visually across light/dark mode, EN/FR/AR languages, and mobile viewport.
</verification>

<success_criteria>
- Why LIGHTLAB section (section 8 of 9) exists in DOM after Testimonials
- 5 pillar cards render with title + copy
- Hero primary CTA: "Start a Project" (copy.home.ctaButtonStart); Hero secondary CTA: "View Our Work" (copy.home.heroCtaSecondary)
- Final CTA primary button: "Start a Project" (copy.home.ctaButtonStart, mailto); secondary: "Book a Diagnostic" (copy.home.ctaButton2, /contact)
- Full 9-section page order matches spec — no METHOD section
- All copy from i18n — zero hardcoded user-facing strings
- Zero TypeScript build errors
- Premium visual quality: generous whitespace, consistent typography, correct color tokens
- Works in light and dark theme
- Works in LTR and RTL
</success_criteria>

<output>
After completion, create `.planning/phases/01-home-redesign/01-home-redesign-03-SUMMARY.md` following the summary template at `@C:/Users/PC/.claude/get-shit-done/templates/summary.md`.
</output>
