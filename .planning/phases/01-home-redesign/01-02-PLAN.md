---
phase: 01-home-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - src/pages/Home.tsx
  - src/i18n.tsx
autonomous: true
requirements:
  - HOW-WE-WORK-4-STEPS
  - SERVICES-4-PILLARS

must_haves:
  truths:
    - "How We Work section shows exactly 4 numbered steps: Architecture, Build, Deploy, Scale"
    - "Services section shows 4 cards (AI Systems, Custom SaaS, Growth Infrastructure, Performance Marketing)"
    - "How We Work step connector line between cards communicates flow / sequence"
    - "The METHOD section (methodCards) is removed from Home.tsx — it does not appear in the rendered page"
    - "Stats section has id='stats' so it can be identified by DOM queries and checkpoint verification"
    - "All new copy is sourced from i18n, not hardcoded"
    - "Page compiles and loads without TypeScript or runtime errors"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "How We Work section with 4 steps; Services grid updated to 4 columns; METHOD LazySection removed; Stats LazySection has id='stats'"
      contains: "HOW WE WORK"
    - path: "src/i18n.tsx"
      provides: "howWeWork translations (4 steps) for en/fr/ar"
      contains: "howWeWork"
  key_links:
    - from: "src/pages/Home.tsx HowWeWork section"
      to: "src/i18n.tsx copy.home.howWeWork"
      via: "useLanguage() hook"
      pattern: "copy\\.home\\.howWeWork"
    - from: "Services grid"
      to: "copy.home.serviceTracks[3]"
      via: "array map"
      pattern: "serviceTracks\\.map"
---

<objective>
Add the "How We Work" section (section 4 of 9), update the Services section to render all 4 service pillars, remove the legacy METHOD section, and add an id to the Stats section so the final page has exactly 9 identifiable sections as specified.

Purpose: The 4-step process (Architecture → Build → Deploy → Scale) is a core conversion element that shows prospects exactly how LIGHTLAB operates. Services currently shows 3 tracks but the spec and i18n (updated in plan 01) have 4. The existing METHOD section overlaps conceptually with HowWeWork and must be removed to achieve the correct 9-section order. The Stats section needs an id attribute for reliable DOM identification during verification.
Output: A new HowWeWork inline component in Home.tsx, updated Services grid to 4 columns, METHOD LazySection removed, Stats LazySection gets id='stats', and i18n keys for the process steps.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-home-redesign/01-CONTEXT.md
@src/pages/Home.tsx
@src/components/PremiumUI.tsx
@src/i18n.tsx
@.planning/phases/01-home-redesign/01-home-redesign-01-SUMMARY.md

<interfaces>
<!-- After plan 01, i18n.tsx already has: -->
```typescript
copy.home.ctaButtonStart          // 'Start a Project' (added in plan 01)
copy.home.projectsSectionTitle    // added in plan 01
copy.home.viewAllLabel            // added in plan 01
copy.home.trustSignals            // added in plan 01
copy.home.serviceTracks           // now 4 entries (4th 'Performance Marketing' added in plan 01)
```

<!-- New i18n shape to add in this plan -->
```typescript
copy.home.howWeWork: {
  label: string          // eyebrow, e.g. 'Our Process'
  headline: string       // e.g. 'How we build.'
  steps: Array<{
    number: string       // '01', '02', '03', '04'
    title: string        // 'Architecture', 'Build', 'Deploy', 'Scale'
    copy: string         // 1-sentence description
  }>
}
```

<!-- Services section currently renders (after plan 01 the 4th card data exists but grid is still 3-col): -->
```tsx
<div className="grid grid-cols-1 md:grid-cols-3 gap-4">
  {copy.home.serviceTracks.map((track, i) => ( ... ))}
</div>
```
// Change to: md:grid-cols-2 lg:grid-cols-4 to handle 4 cards gracefully

<!-- Stats LazySection currently has no id (around line 463 in pre-plan-01 Home.tsx): -->
```tsx
<LazySection>   ← change to: <LazySection id="stats">
```

<!-- METHOD section to REMOVE (lines ~475-533 in pre-plan Home.tsx): -->
<!-- The entire <LazySection id="method"> block including its border-t wrapper div, -->
<!-- the header, the methodCards grid, and the methodCopy paragraph. -->
<!-- The i18n keys (copy.home.methodTitle, copy.home.methodCards, etc.) are NOT removed from -->
<!-- i18n.tsx — they are still used by other pages. Only the JSX in Home.tsx is removed. -->

<!-- HowWeWork component must be defined ABOVE the Home function. -->
<!-- It receives all data as props — no closure over Home's local variables. -->
interface HowWeWorkProps {
  isDark: boolean
  isArabic: boolean
  P: typeof LT          // same shape as DK
  dir: { text: string; flex: string }
  copy: ReturnType<typeof useLanguage>['copy']
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add howWeWork i18n keys</name>
  <files>src/i18n.tsx</files>
  <action>
In src/i18n.tsx, add `howWeWork` to the `home` namespace in ALL THREE locales (en, fr, ar). Place it after `trustSignals` (added in plan 01). Add only — do not rename or remove existing keys.

EN:
```
howWeWork: {
  label: 'Our Process',
  headline: 'How we build.',
  steps: [
    { number: '01', title: 'Architecture', copy: 'Deep diagnostic of your systems and revenue levers. We define the technical blueprint and KPIs before writing a single line of code.' },
    { number: '02', title: 'Build', copy: 'Iterative sprints with weekly deliverables. Full-stack execution from data models to polished interfaces.' },
    { number: '03', title: 'Deploy', copy: 'Zero-downtime launches with monitoring, error tracking, and performance baselines established from day one.' },
    { number: '04', title: 'Scale', copy: 'Continuous optimization, A/B testing infrastructure, and system expansion as your product grows.' },
  ],
},
```

FR:
```
howWeWork: {
  label: 'Notre Processus',
  headline: 'Comment nous construisons.',
  steps: [
    { number: '01', title: 'Architecture', copy: 'Diagnostic approfondi de vos systèmes et leviers de croissance. Nous définissons le blueprint technique avant d\'écrire la première ligne de code.' },
    { number: '02', title: 'Construction', copy: 'Sprints itératifs avec des livrables hebdomadaires. Exécution full-stack des modèles de données aux interfaces finalisées.' },
    { number: '03', title: 'Déploiement', copy: 'Lancements sans interruption avec surveillance, suivi des erreurs et benchmarks de performance dès le premier jour.' },
    { number: '04', title: 'Croissance', copy: 'Optimisation continue, infrastructure de tests A/B et expansion du système au rythme de votre croissance.' },
  ],
},
```

AR:
```
howWeWork: {
  label: 'عمليتنا',
  headline: 'كيف نبني.',
  steps: [
    { number: '01', title: 'الهندسة المعمارية', copy: 'تشخيص عميق لأنظمتك ورافعات الإيرادات. نحدد المخطط التقني ومؤشرات الأداء قبل كتابة سطر واحد من الكود.' },
    { number: '02', title: 'البناء', copy: 'سبرينت تكرارية مع تسليمات أسبوعية. تنفيذ متكامل من نماذج البيانات إلى الواجهات المصقولة.' },
    { number: '03', title: 'النشر', copy: 'إطلاق بدون توقف مع المراقبة وتتبع الأخطاء وخطوط الأداء الأساسية المحددة من اليوم الأول.' },
    { number: '04', title: 'التوسع', copy: 'تحسين مستمر وبنية تحتية لاختبار A/B وتوسيع النظام مع نمو منتجك.' },
  ],
},
```
  </action>
  <verify>
    <automated>cd /c/Users/PC/Documents/Github/lightlab && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. `copy.home.howWeWork.steps` has 4 items in all three locales.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix Services grid, add id to Stats section, remove METHOD section, build HowWeWork section</name>
  <files>src/pages/Home.tsx</files>
  <action>
Make four targeted changes to Home.tsx in a single edit pass. Complete all four changes before saving the file to avoid conflicts.

**Change A — Fix Services grid for 4 cards:**
Find the Services section grid div (currently `grid grid-cols-1 md:grid-cols-3 gap-4`). Change it to:
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
```
The card JSX itself requires no changes — it already maps over `copy.home.serviceTracks` which now has 4 entries from plan 01.

**Change B — Add id='stats' to the Stats LazySection:**
The Stats section currently opens with `<LazySection>` (no id attribute). Change it to:
```tsx
<LazySection id="stats">
```
This makes the section identifiable by DOM id for verification purposes.

**Change C — Remove the METHOD LazySection block:**
The METHOD section currently sits between the STATS section and the TESTIMONIALS section. It starts with:
```tsx
{/* ══════════════════════════ METHOD ══════════════════════════ */}
<LazySection id="method">
```
and ends just before:
```tsx
{/* ══════════════════════════ TESTIMONIALS ══════════════════════════ */}
```

Remove the entire block: the comment, the `<LazySection id="method">`, its entire inner content (the border-t div, the header with `methodTitle`/`expertiseTitle`, the `methodCards` grid, the `methodCopy` paragraph), and the closing `</LazySection>`.

Do NOT touch the STATS, TESTIMONIALS, or any other section. Do NOT remove any i18n keys — `copy.home.methodTitle`, `copy.home.methodCards`, and `copy.home.methodCopy` stay in i18n.tsx (they may be used by other pages). Only the JSX block in Home.tsx is deleted.

**Change D — Build HowWeWork section and insert it:**
Create a new inline memo component `HowWeWork` (above the Home function, below the AnimatedCounter component). It receives all data as props — defined outside Home, it cannot use closure over Home's local variables.

Props interface:
```typescript
interface HowWeWorkProps {
  isDark: boolean
  isArabic: boolean
  P: typeof LT
  dir: { text: string; flex: string }
  copy: ReturnType<typeof useLanguage>['copy']
}
```

Insert `<HowWeWork isDark={isDark} isArabic={isArabic} P={P} dir={dir} copy={copy} />` in the JSX between the Services `</LazySection>` and the Projects `<LazySection id="projects">`.

HowWeWork layout:
- Outer: `<LazySection id="how-we-work">`
- Inner wrapper: `border-t ${P.border}` then `max-w-7xl mx-auto px-6 md:px-12 lg:px-20 py-20 md:py-28`
- Header block (same pattern as other sections):
  ```tsx
  <div className={`mb-12 ${dir.text}`}>
    <TextReveal>
      <span className={`text-[9px] font-bold tracking-[0.52em] uppercase block mb-3 ${P.muted}`}>
        {copy.home.howWeWork.label}
      </span>
    </TextReveal>
    <SplitText
      text={copy.home.howWeWork.headline}
      className={`text-3xl md:text-4xl font-display font-light leading-[1.1] ${isDark ? 'text-white/90' : 'text-[#18160f]'}`}
      type="words"
    />
  </div>
  ```

- Steps grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4`
- Each step card (map over `copy.home.howWeWork.steps`):
  ```tsx
  <div className={`relative flex flex-col gap-5 p-8 rounded-2xl border
    ${isDark ? `${P.cardBg} ${P.border}` : `${P.cardBg} ${P.border}`}`}>
    {/* Step number */}
    <span className={`font-mono text-[9px] tracking-[0.44em] uppercase ${isDark ? 'text-white/20' : 'text-[#8c8780]'}`}>
      {step.number}
    </span>
    {/* Connector line (visual flow indicator) */}
    <span className={`h-px w-12 ${isDark ? 'bg-white/10' : 'bg-[#d9d5ca]'}`} />
    {/* Title */}
    <h4 className={`text-xl md:text-2xl font-display font-light leading-tight
      ${isDark ? 'text-white/90' : 'text-[#18160f]'}`}>
      {step.title}
    </h4>
    {/* Description */}
    <p className={`text-sm font-light leading-[1.72] flex-1
      ${isDark ? 'text-white/40' : 'text-[#56514a]'}`}>
      {step.copy}
    </p>
    {/* Scale badge on last card */}
    {step.number === '04' && (
      <div className={`inline-flex self-start items-center gap-2 px-3 py-1.5 rounded-full text-[9px] font-bold tracking-[0.3em] uppercase border
        ${isDark ? 'border-white/[0.08] text-white/30' : 'border-[#d9d5ca] text-[#8c8780]'}`}>
        → Scale
      </div>
    )}
  </div>
  ```

DO NOT change Hero, Trust Signals, Projects section content, Testimonials, or CTA sections.
  </action>
  <verify>
    <automated>cd /c/Users/PC/Documents/Github/lightlab && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes. In the browser:
- Services section shows 4 cards in a 2-col (md) / 4-col (lg) grid
- Stats section has id="stats" in the DOM (grep for `id="stats"` in Home.tsx returns one result)
- "How We Work" section with 4 numbered steps appears between Services and Featured Projects
- Steps are titled Architecture, Build, Deploy, Scale with step numbers 01–04
- The METHOD section is gone from the page (grep for `id="method"` in Home.tsx returns no results)
- Language toggle works for all three languages
- Light and dark themes both render correctly
  </done>
</task>

</tasks>

<verification>
npm run build passes with zero errors. Home page at localhost:5173 shows: Services with 4 cards, a "How We Work" section with 4 numbered step cards (Architecture → Build → Deploy → Scale) between Services and Featured Projects, no METHOD section anywhere on the page, and Stats section identifiable by id="stats".
</verification>

<success_criteria>
- Services grid renders 4 cards (AI Solutions, Development, Media Buying, Performance Marketing)
- How We Work section (section 4 of 9) exists in DOM between Services and Projects
- 4 step cards with numbers 01–04, titles Architecture / Build / Deploy / Scale
- Stats LazySection has id="stats" attribute
- METHOD LazySection is removed from Home.tsx (grep for id="method" returns no results in Home.tsx)
- All copy from i18n — tested by switching to FR and AR in the language selector
- Zero TypeScript build errors
- Works in light and dark theme
- Works in LTR and RTL
</success_criteria>

<output>
After completion, create `.planning/phases/01-home-redesign/01-home-redesign-02-SUMMARY.md` following the summary template at `@C:/Users/PC/.claude/get-shit-done/templates/summary.md`.
</output>
